<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multi.multi_semi.ai_image.dao.AiImgMapper">

    <insert id="insertAiImg" parameterType="com.multi.multi_semi.ai_image.dto.AiImgDto">
        INSERT INTO ai_img (mem_email, org_url, thum_url, created_at)
        VALUES (#{memEmail}, #{orgUrl}, #{orgUrl}, NOW())
    </insert>

    <select id="findByEmail" resultType="com.multi.multi_semi.ai_image.dto.AiImgDto">
        SELECT
            mem_email, org_url
        /*, created_at (만약 생성일 컬럼이 있다면) */
        FROM
            ai_img
        WHERE
            mem_email = #{memEmail}
        ORDER BY
            created_at DESC /* (최신순 정렬 권장) */
    </select>

</mapper>